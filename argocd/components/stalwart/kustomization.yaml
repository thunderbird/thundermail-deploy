---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: thundermail

# Listed in sync wave order
resources:
  - secret.yaml
  - configmap.yaml
  - ingress.yaml
  - service.yaml
  - statefulset.yaml

commonLabels:
  app: stalwart