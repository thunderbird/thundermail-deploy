---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: stalwart
  namespace: thundermail
spec:
  minReplicas: 1
  maxReplicas: 1
  # Below are the correct values, currently overriden for a test
  # minReplicas: 2
  # maxReplicas: 3
